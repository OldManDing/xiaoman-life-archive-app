# 用户问题执行闭环清单（2026-05-11）

## 执行原则

- 按“先上线阻断、再核心链路、再 UI 精修、最后发布与运维”的优先级闭环。
- 每个问题必须对应代码、文档、脚本或测试证据；无法在本机闭环的外部事项明确标为“外部待验证”。
- App 当前是移动端 Web/H5 形态，不是 Android 或 iOS 原生包；后续 App 化作为独立待办进入发布后任务。

## 问题到任务映射

| 序号 | 用户问题 | 执行任务 | 当前状态 | 闭环证据 |
| --- | --- | --- | --- | --- |
| 1 | 根据《后台 UI 与 API 开发任务》看到哪里了 | 复核后台页面、后台 API、详情抽屉、审核操作、审计筛选 | 已闭环 | `docs/后台UI与API开发任务_2026-05-10.md` 已更新；E2E 后台 5 条通过 |
| 2 | 继续完成剩下的后台 UI/API | 补齐后台详情、状态操作、AI 任务、审计日志闭环 | 已闭环 | 后台 controller/service 与 adminApi 已对齐；`npm run test:e2e` 通过 |
| 3 | 是否用了“瑜伽”的后台组件 | 确认后台技术栈和组件来源 | 已确认 | 当前后台为 React + Vite 自研组件，不是 Umi/Ant Design 组件库 |
| 4 | App 界面精修、全部中文化，提示语也要中文 | 扫描 App/Admin 文案、错误提示、E2E 文案；移除短信登录和接受邀请页残留 | 已闭环 | E2E 已改为账号密码；截图见 `artifacts/visual-review-current/` |
| 5 | 检查前后台功能是否对齐 | 对齐 App API、后台 API、种子数据、页面入口和 E2E | 已闭环 | `webApi`、`adminApi`、`prisma/seed.ts` 与 12 条 E2E 通过 |
| 6 | 所有功能和页面是否完善、是否还要测试或修改 | 跑生产模板校验、审计、回归、构建、E2E、浏览器视觉抽检 | 已闭环 | 本轮全部本地门禁通过 |
| 7 | 按优先级把四步全部做完 | 优先级：认证 -> 配置部署 -> UI/API 对齐 -> 测试发布清单 | 已闭环 | 本清单与 `docs/实施版_上线闭环检查清单.md` 已更新 |
| 8 | 离上线标准还差多少 | 更新上线差距和阻塞项 | 已闭环 | 剩余为真实生产域名/密钥/provider/法务主体/监控演练等外部上线项 |
| 9 | App 是 Android 还是 iOS，短信有没有免费 | 明确当前 App 形态与短信策略 | 已处理 | 当前是 H5/Web；短信默认关闭，不作为上线依赖 |
| 10 | 后续做成 App，设置成待办任务 | 建立 App 化待办项 | 已纳入 | 本文“发布后待办” |
| 11 | 干净发布版本 | 整理工作区、跑 CI 同等门禁、最终给出是否可提交 | 已闭环 | `npm run regression:baseline`、三端 build、E2E 全部通过，提交后工作区应保持干净 |
| 12 | 生产配置闭环 | 校验 `.env.example`、`.env.local.example`、生产校验脚本和运行时校验 | 已闭环 | `verify:production-template`、生产 Compose 配置、运行时 JWT 强度校验通过 |
| 13 | 真实 provider 全链路验证 | 对象存储、AI 生产必填；短信关闭；真实账号环境标为外部待验证 | 代码闭环，外部待验证 | 本地只能验证 mock/模板；真实服务需账号密钥 |
| 14 | 部署方案完整 | 校验生产 Compose、Nginx、健康检查、备份恢复、回滚文档 | 已闭环 | `deploy/docker-compose.prod.yml`、`deploy/ops/*`、Compose config 通过 |
| 15 | 合规与用户侧文档 | 用户协议、隐私政策、儿童信息保护规则补齐 | 已实现，需主体信息 | `docs/legal/*.md`，上线前补经营主体与联系方式 |
| 16 | 运维上线项闭环 | 备份、健康检查、日志、审计、监控告警、迁移回滚 | 已实现，需演练 | `docs/实施版_生产部署运行手册.md`、`deploy/ops/*` |
| 17 | App 登录改为账号密码 + 邀请码，不用短信 | 后端关闭短信登录、前端登录改账号密码 | 已闭环 | API/Web 单测与 E2E 密码登录通过 |
| 18 | 注册功能必须有 | 增加 `/auth/register` 和前端注册模式 | 已闭环 | `RegisterDto`、`AuthController.register`、Web/API 测试通过 |
| 19 | 邀请码只用于注册，注册后账号密码登录 | 注册消费邀请码加入家庭；登录只校验账号密码；移除登录后接受邀请入口 | 已闭环 | `AuthService.createPasswordAccountWithInvite`、API smoke、家庭邀请码 E2E 通过 |

## 本轮验证结果

| 命令 / 检查 | 结果 |
| --- | --- |
| `npm run verify:production-template` | 通过 |
| `docker compose -f deploy/docker-compose.prod.yml --env-file .env.example config --quiet` | 通过 |
| `git diff --check` | 通过 |
| `npm audit --omit=dev --audit-level=moderate` | 通过，0 漏洞 |
| `npm run regression:baseline` | 通过 |
| `npm run build:api` | 通过 |
| `npm run build:web` | 通过 |
| `npm run build:admin` | 通过 |
| `npm run test:e2e` | 通过，12/12 |
| 浏览器截图抽检 | 通过，已固化为 `e2e/visual.spec.ts`，截图见 `artifacts/visual-review-current/` |

## 本轮修复记录

- 修复 E2E 中短信验证码登录残留，改为 `xiaoman_parent` + `DemoUser123!` 账号密码登录。
- 修复 E2E 文案定位过宽导致 `小满` 多元素匹配的问题。
- 修复生产 Compose 未传递 `SMS_ENABLED` 的问题。
- 修复短信关闭时健康检查仍解析短信 provider 的问题，健康检查现在返回 `sms=disabled`。
- 将生产 `.env.example` 的短信可选字段改为显式空值，避免 Compose 缺变量警告，同时保持短信默认关闭。
- 修正测试环境手册，明确本地应复制 `.env.local.example`，生产/预发才使用 `.env.example`。
- 修复“邀请码只用于注册”的残留冲突，移除 App 接受邀请页、邀请链接入口和 `/invites/:token/accept` 前端调用，并更新家庭协作测试。
- 补运行时生产 JWT 强度校验：生产/预发环境会拒绝短密钥以及访问令牌、刷新令牌共用同一密钥。
- 新增 `e2e/visual.spec.ts`，覆盖 App 登录页、首页、邀请码页和后台总览、媒体页截图及横向溢出检查。

## 发布后待办

- App 化：评估 Capacitor/Tauri/React Native/Flutter 路线，至少输出 Android 包优先还是 iOS 包优先、推送/相册/登录态/应用商店材料清单。
- 真实 provider 预发演练：用真实对象存储、真实 AI key 和真实域名跑上传、签名访问、AI 任务。
- 法务与合规定稿：补经营主体、联系方式、投诉渠道、儿童信息保护负责人。
- 监控告警接入：落地 Uptime/云监控/Prometheus 任一方案，并演练告警触达。

## 本轮复测命令

```bash
npm run verify:production-template
docker compose -f deploy/docker-compose.prod.yml --env-file .env.example config --quiet
npm audit --omit=dev --audit-level=moderate
npm run regression:baseline
npm run build:api
npm run build:web
npm run build:admin
npm run test:e2e
```
